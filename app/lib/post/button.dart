import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

import 'category_badge.dart';

// create a button with rounded corners and a button and text 'New' and a color of Colors.blue[100]

class Button extends StatelessWidget {
  final String text;
  final Color color;
  final Function onPressed;
  final IconData icon;

  const Button(
      {Key? key,
      required this.text,
      required this.color,
      required this.onPressed,
      required this.icon})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    // return a button with rounded corners and a button and text 'New' and a color of Colors.blue[100]
    return 
    );
  }
}
